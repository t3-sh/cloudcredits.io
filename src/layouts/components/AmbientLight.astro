<div class="ambient-light-container">
  <div class="ambient-light-effect light-1"></div>
  <div class="ambient-light-effect light-2"></div>
  <div class="ambient-light-effect light-3"></div>
</div>

<style>
  .ambient-light-container {
    position: absolute;
    width: 100%;
    height: 100%;
    overflow: hidden;
    z-index: 1;
    pointer-events: none;
  }

  .ambient-light-effect {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.5;
    animation: float 20s ease-in-out infinite;
  }

  .light-1 {
    width: 40vw;
    height: 40vw;
    background: radial-gradient(
      circle,
      rgba(125, 39, 255, 0.4) 0%,
      rgba(125, 39, 255, 0) 70%
    );
    top: 30%;
    left: 15%;
    animation-delay: 0s;
  }

  .light-2 {
    width: 35vw;
    height: 35vw;
    background: radial-gradient(
      circle,
      rgba(0, 183, 255, 0.4) 0%,
      rgba(0, 183, 255, 0) 70%
    );
    top: 40%;
    right: 15%;
    animation-delay: -5s;
  }

  .light-3 {
    width: 30vw;
    height: 30vw;
    background: radial-gradient(
      circle,
      rgba(255, 61, 245, 0.3) 0%,
      rgba(255, 61, 245, 0) 70%
    );
    bottom: 20%;
    left: 35%;
    animation-delay: -10s;
  }

  @keyframes float {
    0% {
      transform: translate(0, 0) scale(1);
    }
    25% {
      transform: translate(5%, 5%) scale(1.05);
    }
    50% {
      transform: translate(0%, 10%) scale(1);
    }
    75% {
      transform: translate(-5%, 5%) scale(0.95);
    }
    100% {
      transform: translate(0, 0) scale(1);
    }
  }

  /* Dark mode adjustments */
  :global(.dark) .light-1 {
    background: radial-gradient(
      circle,
      rgba(125, 39, 255, 0.5) 0%,
      rgba(125, 39, 255, 0) 70%
    );
  }

  :global(.dark) .light-2 {
    background: radial-gradient(
      circle,
      rgba(0, 183, 255, 0.5) 0%,
      rgba(0, 183, 255, 0) 70%
    );
  }

  :global(.dark) .light-3 {
    background: radial-gradient(
      circle,
      rgba(255, 61, 245, 0.4) 0%,
      rgba(255, 61, 245, 0) 70%
    );
  }

  /* Media query for smaller screens */
  @media (max-width: 768px) {
    .light-1,
    .light-2,
    .light-3 {
      filter: blur(60px);
    }
  }
</style>
