export interface StatGroupItem {
  title: string;
  value: string;
  desc?: string;
}

export interface Provider {
  name: string;
  short_name: string;
  slug: string;
  color: string;
  intro: string;
  url: string;
  video: string;
  image: string;
}
